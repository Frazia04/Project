export const standardLibrary = [
    "let min (a: Nat) (b: Nat) = if a <= b then a else b", // min
    "let max (a: Nat) (b: Nat) = if a <= b then b else a", // max
    "let not (a: Bool) = if a then false else true", // not
]